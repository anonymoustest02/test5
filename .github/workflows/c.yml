name: Job C

on:
  workflow_run:
    workflows: ["Job A"]
    types:
      - completed

jobs:
  C:
    runs-on: self-hosted
    steps:
      - run: echo "Job C running" && sleep 10
       
      - name: Checkout code
        uses: actions/checkout@v3

     
